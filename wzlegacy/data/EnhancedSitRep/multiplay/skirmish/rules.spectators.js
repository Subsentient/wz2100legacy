// rules.spectators.js v0.1
//
// Return true if player is a spectator
//
// Used by rules.js and rules.init.js, possibly others.
//
// /////////////////////////////////////////////////////////////////


function isSpectator(player) {
	// Example:
	// if (player == 3 || player == 6) return true; // player ID's 3 and 6 are spectators
    
    return false; // not a spectator
}